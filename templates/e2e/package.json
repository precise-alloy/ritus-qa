{
  "name": "e2e",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "npx playwright test",
    "report": "npx playwright show-report",
    "project": "npx playwright test --project",
    "verify-cms": "npx playwright test --grep \"@CMS\"",
    "update-browser": "npx playwright install"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "dotenv": "^16.0.3",
    "typescript": "^5.5.0",
    "eslint": "^8.57.0",
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "prettier": "^3.3.0"
  }
}
